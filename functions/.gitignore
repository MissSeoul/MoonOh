// Git이 특정 파일 또는 디렉토리를 버전 관리에서 제외하도록 설정하는 파일
//Git에 올리고 싶지 않은 파일을 .gitignore에 추가하면, Git이 이를 무시하고 커밋하지 않도록 설정
// 개인정보 보호 → API 키, 데이터베이스 비밀번호 등이 포함된 .env 파일을 Git에 올리지 않도록 설정
//불필요한 파일 제외 → node_modules, build 디렉토리 같은 자동 생성되는 파일들을 제외하여 저장소를 깔끔하게 유지
//배포 시 불필요한 설정 파일 제외 → OS에서 생성하는 캐시 파일이나 IDE 설정 파일(.DS_Store, .vscode) 등을 제외

node_modules/
*.local
